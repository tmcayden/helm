{
  "name": "@helm/cli",
  "version": "0.1.0",
  "description": "Helm's terminal edition: the pty host for Claude Code sessions, over @helm/core.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "helm": "dist/helm.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@helm/core": "workspace:*",
    "better-sqlite3": "^13.0.3",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
